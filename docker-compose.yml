services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: Kaspi
    ports:
      - "5436:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  backend:
    build: ./back-end
    depends_on:
      - db
    ports:
      - "8011:8000"

  frontend:
    build: ./front-end
    ports:
      - "5174:5173"
    depends_on:
      - backend

volumes:
  db_data:
